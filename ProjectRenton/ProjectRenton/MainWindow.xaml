<Window
    x:Class="ProjectRenton.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ProjectRenton"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d">

    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
        <ListView x:Name="list"></ListView>
        <Button x:Name="myButton" Click="myButton_Click">Click Me</Button>
        <Button x:Name="handButton" Click="handButton_Click">Toggle Hand Raised</Button>
        <Button Name="ButtonRun"
                    ToolTipService.ToolTip="Pick and evaluate and image"
                    Width="100"
                    Height="40"
                    Click="ButtonRun_Click" 
                    IsEnabled="True" 
                    VerticalAlignment="Bottom" RenderTransformOrigin="2.085,7.842" Margin="84,0,0,654">
            <Button.Content>
                Pick image
            </Button.Content>
        </Button>
        <Image x:Name="CameraVisual" />
    </StackPanel>
</Window>
